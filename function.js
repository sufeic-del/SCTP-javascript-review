function addTwo(number1, number2) {
    let answer = number1 + number2;
    return answer;
}
let ans =addTwo(4.5);
console.log(ans);
//but a function can do data processing
// in other words a function can receive data and give back processed data

// a function given two numbers as data and the result is their sum
//1. define function name
//2. put the received data between the () -- assign the received data any variable you want
//3. arrive at the answer
//4. return the answer (the result of the function)
function addTwo(number1, number2){
    let answer = number1 + number2;
    return answer;